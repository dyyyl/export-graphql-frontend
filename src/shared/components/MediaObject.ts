import styled from 'styled-components/macro';

const MediaObject = styled.span`
  display: grid;
  grid-template-columns: max-content max-content;
  gap: 2rem;

  place-items: center;
`;

export default MediaObject;
